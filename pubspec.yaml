name: noor_pro
description: Noor Pro - offline-first Quran and Islamic knowledge app.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.28.0
  sqlite3_flutter_libs: ^0.5.30
  path_provider: ^2.1.5
  path: ^1.9.1
  crypto: ^3.0.6
  http: ^1.4.0
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.28.0
  build_runner: ^2.5.4
  lints: ^6.0.0

flutter:
  uses-material-design: true
